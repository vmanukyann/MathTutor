name: MathTutor
description: "A math tutoring application."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  
  # Existing dependencies
  camera: ^0.10.5+5
  http: ^1.1.2
  path_provider: ^2.1.1
  flutter_math_fork: ^0.7.2
  audioplayers: ^5.2.1
  cupertino_icons: ^1.0.8
  image_picker: ^1.0.7
  supabase_flutter: ^2.5.6
  firebase_core: ^3.3.0
  firebase_auth: ^5.1.4
  shared_preferences: ^2.2.3
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
